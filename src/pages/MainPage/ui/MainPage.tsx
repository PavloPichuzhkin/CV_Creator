import React, {useRef} from 'react';

const MainPage = (props:any) => {
    const ref=useRef()
   return <main ref={ref}>
    {/*return <main >*/}
    MainPage {'/n'}
       mk
    </main>
};

export default MainPage;
