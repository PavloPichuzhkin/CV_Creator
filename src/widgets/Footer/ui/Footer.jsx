import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <footer>
        <div>
Footer
        </div>
        </footer>
    );
};

Footer.propTypes = {

};

export default Footer;
